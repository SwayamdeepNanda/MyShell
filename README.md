MyShell
Production-ready custom shell implementation (Phase 1 bootstrap)

Project goals (Phase 1)
- Provide a modular codebase scaffold for a production-ready shell.
- Setup repos, formatting, and basic documentation.
- Provide reproducible build and QA workflows (Makefile/CMake next).

Directory layout
- src/ — C source files
- include/ — public headers
- plugins/ — dynamic command plugins
- tests/ — unit & integration tests
- docs/ — user & architecture documentation
